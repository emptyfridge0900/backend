name: Deploy Full App to Fly.io

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # Set up .NET
      - name: Set up .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '9.x'

      # Build .NET backend
      - name: Build Book.Server
        working-directory: ./Book.Server
        run: |
          dotnet restore
          dotnet build --configuration Release

      # Set up Node.js
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20.x'

      # Build React frontend
      - name: Build React Frontend
        working-directory: ./book.client
        run: |
          npm ci
          npm run build
          ls -lR ./dist  # Adjust to ./build if needed

      # Copy frontend to backend wwwroot
      - name: Prepare Deployment
        run: |
          mkdir -p Book.Server/wwwroot
          cp -r book.client/dist/* Book.Server/wwwroot/
          ls -lR Book.Server/wwwroot

      # Deploy to Fly.io
      - uses: superfly/flyctl-actions/setup-flyctl@master
      - name: Deploy to Fly.io
        run: flyctl deploy --remote-only
        env:
          FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}